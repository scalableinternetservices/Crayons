<body style= "padding-top: 50px">
<h1> <%= @profile.firstname %> <%= @profile.lastname %></h1>
<h3><%= @profile.email %></h3>

<h1> My Human Capital: <%= @my_human_capital %></h1>

<p>Number of photos uploaded: <%= @profile.number_of_images_uploaded %></p>
<p>Number of photos downloaded: <%= @profile.number_of_images_downloaded %></p>
<p>Number of tags made: <%= @profile.number_of_labels_made %></p>
<p>Number of times you upvoted: <%= @profile.number_of_upvotes%></p>
<p>Number of times you downvoted: <%= @profile.number_of_downvotes%></p>
<p>Number of times your images have been upvoted: <%= @profile.number_of_upvotes_on_tag%></p>
<p>Number of times your images have been downvoted: <%= @profile.number_of_downvotes_on_tag%></p>


<li><%= link_to "Edit Account", '/user_devises/edit' %>


  <div class="media">

    <h3> Your Photos </h3>

    <% @photos.each do |photo| %>
        <%if photo.user_devises_id == current_user_devise.id %>
        <h4 class="media-heading"><%= photo.title %></h4>
        <div class="media-left">
          <!-- Renders image -->
          <%= link_to image_tag(photo.image.url, class: 'media-object'), photo.image.url, target: '_blank' %>
          <!-- Renders Annotation-->
          <!-- rendering stuff here photo.annotation.url, class: 'text-field?'-->
          <!-- Button to delete image -->
          <p style= "padding-top: 2px"></p>
          <%= link_to 'Remove', photo_path(photo), class: 'btn btn-danger', method: :delete, data: {confirm: 'Are you sure?'} %>

          <!-- So this section is creating a new empty annotation for each photo and if the user decides to -->

          <%# @new_ann = Annotation.create(params[:photo_id]) %>

          <%#  binding.pry %>

          <p style= "padding-top: 5px"></p>
        </div>

        <div class="media-body">

        </div>

    <% end %>
    <% end %>
  </div>
</body>
